using System;
using System.Collections.Generic;
using System.Text;

namespace ejercicios.Dados
{
    /*EXPLICACION:
     Hice una clase usuario que realiza apuestas, una clase para el dado y una clase de comapracion más esta clase.*/
    class main
    {
        /*static void Main(string[] args)
        {
            Dado nuevoDado = new Dado();
            Usuario nuevoUsuario = new Usuario();
            Comparar nuevoComparador = new Comparar();
            nuevoUsuario.generarApuesta();
            nuevoDado.rodar();
            Console.WriteLine("Apuesta: " + nuevoUsuario.getApuesta() + " Cara: " + nuevoDado.getCara());
            nuevoComparador.Comparacion(nuevoDado.getCara(), nuevoUsuario.getApuesta());

            Console.WriteLine(nuevoComparador.ToString());
        }*/
    }
}
